#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char awe;

	awe = 'z';
	while
		(awe >= 'a') {
			putchar(awe);
			awe--;
		}
	putchar('\n');
	return (0);
}
