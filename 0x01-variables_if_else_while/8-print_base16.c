#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char awe;

	int h;

	awe = 'a';
	h = 0;
	while
		(h < 10) {
			putchar(h + '0');
			h++;
		}
	while
		(awe <= 'f') {
			putchar(awe);
			awe++;
		}
	putchar('\n');
	return (0);
}
