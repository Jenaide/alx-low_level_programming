#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: string type input
 * Return: length
 */
int _strlen(char *s)
{
	int length = 0;

	while (s[length] != '\0')
		lenght++;
	return (lenght);
}
