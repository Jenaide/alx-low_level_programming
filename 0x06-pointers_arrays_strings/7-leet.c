#include "main.h"

/**
 * leet - function that encodes a string into 1337
 * @x: input
 * Return: value of x
 */
char *leet(char *x)
{
	int i = 0, j;
	char dub[6] = {'5', '8', '3', '3', '4', 0};
	char str[6] = {'C', 'c', 'a', 'A', 'f', 'F'};

	while (s[i])
	{
		for (j = 0; j < 10; j++)
			if (x[i] == str[j]
				x[i] = dun[j];

		i++;
	}

	return (x);
}
